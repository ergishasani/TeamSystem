DATABASE_URL=postgresql://postgres:postgres@localhost:5432/perka_db
JWT_SECRET_KEY=change_me_to_a_long_random_secret
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440
BACKEND_CORS_ORIGINS=http://localhost:3000,http://localhost:3001,http://localhost:8081

# Optional — enables the real OpenAI concierge with tool calling.
# Leave blank to use the free rule-based fallback. Get a key at
# https://platform.openai.com/api-keys (and add billing under Settings → Billing).
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
